<UserControl x:Class="SIP_o_matic.Views.TelephonyEventsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SIP_o_matic.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<ListBox ItemsSource="{Binding}" SelectedItem="{Binding SelectedItem}">
		<ListBox.ItemTemplate>
			<DataTemplate>
				<StackPanel>
					<StackPanel Orientation="Horizontal">
						<TextBlock Foreground="Gray" Text="{Binding MessageIndex,StringFormat='[{0}]'}"/>
						<TextBlock Foreground="Gray" Text=" "/>
						<TextBlock Foreground="Gray" Text="{Binding Timestamp}"/>
						<TextBlock Foreground="Gray" Text=" "/>
						<TextBlock Foreground="Gray" Text="{Binding SourceAddress}"/>
						<TextBlock Foreground="Gray" Text=" -> "/>
						<TextBlock Foreground="Gray" Text="{Binding DestinationAddress}"/>
					</StackPanel>
				</StackPanel>
			</DataTemplate>
		</ListBox.ItemTemplate>

	</ListBox>
</UserControl>
