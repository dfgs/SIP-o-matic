<UserControl x:Class="SIP_o_matic.Views.MessagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:SIP_o_matic.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

	<views:LadderView Devices="{Binding Devices}" ItemsSource="{Binding Messages}">
		<views:LadderView.ItemTemplate>
			<DataTemplate>
				<views:MessageView Margin="0,0,0,16" ToolTip="{Binding Content}"/>
			</DataTemplate>
		</views:LadderView.ItemTemplate>
	</views:LadderView>

</UserControl>
